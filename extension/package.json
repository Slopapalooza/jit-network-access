{
  "name": "jit-network-access-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chromium MV3 extension: silently answers the JIT Network Access knock so protected origins open transparently.",
  "scripts": {
    "test": "node src/jitcrypto.test.mjs"
  }
}
